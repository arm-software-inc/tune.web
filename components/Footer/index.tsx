import styles from '@styles/components/Footer.module.css';

const Footer = () => {
	return (
		<footer className={styles.footer}>
			<p> footer uahsduash dusahdu</p>
		</footer>
	);
};

export default Footer;
